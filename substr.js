/*
This will method extracts part of the string.
substr( start, length );

 H e l l o
 0 1 2 3 4
*/
var h = "Hello";

console.log( h.substr(1,1) );//this will return e
console.log( h.substr(1,3) );//this will return ell
